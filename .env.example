# Fixtures location (catalog.json / policies.json / budgets.json)
FIXTURES_DIR=fixtures

# Planner selection: rule_based (default, no LLM) | llm (not yet implemented)
PLANNER=rule_based

# Bypass classifier (policy_004 Layer B) selection: rule_based (default,
# soft-keyword heuristic, no LLM) | llm (not yet implemented)
BYPASS=rule_based
